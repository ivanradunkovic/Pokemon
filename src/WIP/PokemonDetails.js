// import React, { Component } from 'react';

// class PokemonDetails extends Component {

// 	render() {
// 		const { id, sprite, name, onChange} = this.props;
// 		return (
// 			<img onClick={() => onChange(id) } src={sprite} alt={name} />
// 		)
// 	}

// }

// export default PokemonDetails;